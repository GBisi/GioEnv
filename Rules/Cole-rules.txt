IF temperature(very_low) DO temperature(very_high).
IF temperature(low) DO temperature(very_high).
IF temperature(high) and outdoor_temperature(low) DO temperature(very_high).
IF temperature(high) and outdoor_temperature(very_low) DO temperature(very_high).
IF temperature(high) DO temperature(high).
IF temperature(very_high) DO temperature(high).

IF light(low) DO light(high).
IF light(medium) DO light(high).

