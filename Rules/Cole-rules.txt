IF temperature(very_low) THEN do(temperature(very_high)).
IF temperature(low) THEN do(temperature(very_high)).
IF temperature(high) and outdoor_temperature(low) THEN do(temperature(very_high)).
IF temperature(high) and outdoor_temperature(very_low) THEN do(temperature(very_high)).
IF temperature(high) THEN do(temperature(high)).
IF temperature(very_high) THEN do(temperature(high)).

IF light(low) THEN do(light(high)).
IF light(medium) THEN do(light(high)).

